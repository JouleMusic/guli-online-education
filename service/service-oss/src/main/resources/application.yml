server:
  port: 8020

spring:
  application:
    name: service-oss

  profiles:
    active: dev

# 阿里云oss
# 不同的服务器，地址不同
aliyun:
  oss:
    file:
      endpoint: oss-cn-shanghai.aliyuncs.com
      keyid:
      keysecret:
      # bucket可以在控制台创建，也可以使用java代码创建
      bucketname: edu-guli-8010
